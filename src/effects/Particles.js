import*as THREE from"three";
export class Particles{constructor(scene){this.scene=scene;this.items=[]}
burst(pos,color=0xffffff,count=24){const g=new THREE.BufferGeometry(),a=new Float32Array(count*3),v=[];for(let i=0;i<count;i++){a[i*3]=pos.x;a[i*3+1]=pos.y;a[i*3+2]=pos.z;v.push(new THREE.Vector3((Math.random()-.5)*4,(Math.random()-.5)*4,(Math.random()-.5)*4))}g.setAttribute("position",new THREE.BufferAttribute(a,3));const m=new THREE.PointsMaterial({color,size:.045,transparent:true,opacity:.9});const p=new THREE.Points(g,m);p.userData={v,life:1};this.scene.add(p);this.items.push(p)}
update(dt){for(let i=this.items.length-1;i>=0;i--){const p=this.items[i],a=p.geometry.attributes.position.array;p.userData.life-=dt;for(let j=0;j<p.userData.v.length;j++){const v=p.userData.v[j];a[j*3]+=v.x*dt;a[j*3+1]+=v.y*dt;a[j*3+2]+=v.z*dt;v.y-=1.5*dt}p.geometry.attributes.position.needsUpdate=true;p.material.opacity=Math.max(0,p.userData.life);if(p.userData.life<=0){this.scene.remove(p);p.geometry.dispose();p.material.dispose();this.items.splice(i,1)}}}}